/**
 * Parte A — Normalización
 * Rama sugerida: feature/normalizacion
 
 */
export function normalizeCharacters(rawCharacters) {
  throw new Error("normalizeCharacters: TODO (feature/normalizacion)");
}
