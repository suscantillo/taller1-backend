/**
 * Parte C — Programación asíncrona
 * Rama sugerida: feature/estadisticas
 *
 */

const BASE_URL = "https://rickandmortyapi.com/api/character";

// Estrategia 1: await dentro de un ciclo, una petición después de la otra.
export async function fetchAllCharactersSequential() {
  throw new Error("fetchAllCharactersSequential: TODO (feature/estadisticas)");
}

// Estrategia 2 (la que debe usar la solución final): Promise.all().
export async function fetchAllCharactersConcurrent() {
  throw new Error("fetchAllCharactersConcurrent: TODO (feature/estadisticas)");
}
