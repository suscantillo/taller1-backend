/**
 * Parte B — Consultas (1 a 5)
 * Rama sugerida: feature/consultas
 */

// 1. filter: vivos ("Alive") y de especie "Human"
export function vivosHumanos(personajes) {
  throw new Error("vivosHumanos: TODO (feature/consultas)");
}

// 2. filter: 20 o más episodios
export function veteranos(personajes) {
  throw new Error("veteranos: TODO (feature/consultas)");
}

// 3. find: primer personaje de especie "Alien" y género "Female"
export function primeraAlienMujer(personajes) {
  throw new Error("primeraAlienMujer: TODO (feature/consultas)");
}

// 4. some: ¿existe al menos un personaje con "tipo" no vacío? -> true/false
export function algunoConTipo(personajes) {
  throw new Error("algunoConTipo: TODO (feature/consultas)");
}

// 5. every: ¿todos tienen imagen y aparecen en al menos 1 episodio? -> true/false
export function todosValidos(personajes) {
  throw new Error("todosValidos: TODO (feature/consultas)");
}
